<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <springProperty
            scope="context"
            name="APP_NAME"
            source="spring.application.name"
            defaultValue="ecommerce-foundation"/>

    <property
            scope="context"
            name="LOG_DIR"
            value="${LOG_DIR:-./logs/${APP_NAME}}"/>

    <appender name="EXECUTION_JSON_FILE"
              class="ch.qos.logback.classic.sift.SiftingAppender">

        <discriminator class="ch.qos.logback.classic.sift.MDCBasedDiscriminator">
            <key>traceType</key>
            <defaultValue>APP</defaultValue>
        </discriminator>

        <sift>

            <appender name="EXECUTION-${traceType}"
                      class="ch.qos.logback.core.rolling.RollingFileAppender">

                <file>${LOG_DIR}/execution/${traceType}.json</file>

                <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

                    <fileNamePattern>
                        ${LOG_DIR}/execution/archive/${traceType}.%d{yyyy-MM-dd}.%i.json.gz
                    </fileNamePattern>

                    <maxFileSize>10MB</maxFileSize>
                    <maxHistory>30</maxHistory>
                    <totalSizeCap>3GB</totalSizeCap>

                </rollingPolicy>

                <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">

                    <providers>

                        <timestamp>
                            <fieldName>@timestamp</fieldName>
                            <timeZone>UTC</timeZone>
                        </timestamp>

                        <logLevel>
                            <fieldName>level</fieldName>
                        </logLevel>

                        <loggerName>
                            <fieldName>logger</fieldName>
                            <shortenedLoggerNameLength>40</shortenedLoggerNameLength>
                        </loggerName>

                        <message>
                            <fieldName>message</fieldName>
                        </message>

                        <mdc>
                            <includeMdcKeyName>requestId</includeMdcKeyName>
                            <includeMdcKeyName>correlationId</includeMdcKeyName>
                            <includeMdcKeyName>traceType</includeMdcKeyName>
                        </mdc>

                        <stackTrace>
                            <fieldName>stack_trace</fieldName>
                        </stackTrace>

                    </providers>

                </encoder>

            </appender>

        </sift>

    </appender>

    <!-- Framework and dependency logs stay quieter -->
    <root level="DEBUG">
        <appender-ref ref="EXECUTION_JSON_FILE"/>
    </root>

</configuration>